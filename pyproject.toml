[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "polar-mcp"
version = "0.1.0"
description = "Polar Flow activity exporter and MCP server"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
